<h1>Criar Oficina</h1>

<div class="layout layout-sm-column">
    {{paper-input id="nome-input" class="flex-30" label="Nome" value=model.name onChange=(action (mut model.name))}}
    {{paper-input id="cnpj-input" class="flex-30" label="CNPJ" value=model.cnpj onChange=(action (mut model.cnpj))}}
</div>

<div class="layout-row">
    {{#paper-button id="create-button" raised="true" primary="true" onClick=(action "create")}}
        Salvar
    {{/paper-button}}

    {{#link-to "workshop"}}
        {{#paper-button id="back-button" raised="true"}}
            Voltar
        {{/paper-button}}
    {{/link-to}}
</div>
